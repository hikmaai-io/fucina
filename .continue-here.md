# Continue here — Qwen3.5-MoE-35B vs vLLM (agentic-coding push)

Branch `qwen35-hybrid` (worktree wt-qwen35). Last commits:
- `addfcd8` perf(batch): per-pass phase telemetry — attribute served-vs-engine gap
- `475b6c5` feat(qwen35): per-conversation state-snapshot cache — turn-2 TTFT 1.47s -> 0.11s
- `c507864` fix(tokenizer): newline runs are one pre-token — \s*[\r\n]+ has priority
- `0d1e531` feat(qwen): ChatML chat dialect — template, XML tool calls, streaming deltas
- (prior) `b035dfd` CUTLASS grouped NVFP4 experts; `df1de77` 16-row LM head; `9b2ff98` 16-row Q4_K GEMV

Goal (session Stop-hook): beat vLLM MoE-35B on prefill AND decode (single + aggregate),
Qwen3.5-35B-A3B-FP8 on the GB10. Standing directives: spec on dense NOT sparse, ship
defaults not flags, no dead code, everything through continuous batching.

## What landed THIS session (all gates green, all live-verified on MoE-35B-A3B-FP8)

1. **THE PLAN item 1 — Qwen ChatML dialect + Hermes/XML tool calling — DONE** (`0d1e531`).
   The functional blocker is gone: a coding agent can now drive the MoE through
   /v1/chat/completions. `internal/chat/dialect.go` introduces a `Dialect` interface;
   `Gemma` wraps the old renderer byte-identically (parity test), `Qwen` (qwen.go)
   reproduces the checkpoint's chat_template.jinja EXACTLY (8 byte-exact goldens rendered
   with the template itself, `scripts/gen_qwen_golden.py`). NOT Hermes JSON — Qwen3.5 uses
   the Qwen3-Coder XML form `<tool_call><function=NAME><parameter=K>V</parameter></function></tool_call>`.
   - Dialect is auto-selected from the vocab (`<|im_start|>` present → Qwen); NO flag.
   - Parser → OpenAI tool_calls with schema-coerced JSON args (order-preserving),
     truncation-safe recovery. tool_choice required/function forcing via prompt-opened
     calls (no grammar needed). Streaming: reasoning_content deltas + per-call streamed
     tool_calls deltas the moment each `</tool_call>` closes, post-call cutoff via ctx
     cancel. All four serving paths (single-flight stream/collect, batch stream/collect).
   - tokenizer: ChatML control-token mapping on BOTH loaders. Fixed real serving bugs on
     the --tokenizer tokenizer.json path (EOS=1/BOS=2/EndOfTurn=106 Gemma defaults in a
     Qwen vocab → spurious stops + junk BOS; added-token ids dropped by Decode).
   - LIVE-VERIFIED: single + parallel tool calls, streaming, tool-loop turn-2, forced choice.

2. **THE PLAN item 2 — hybrid per-conversation state cache — DONE** (`475b6c5`).
   turn-2+ TTFT on a 2k conversation: **1.47s → 0.107s** (measured, MoE-35B). The hybrid
   arch can't use the paged radix prefix tree (GDN state is position-pinned, arenas aren't
   paged), so reuse is EXACT snapshots: save the finished sequence's slot state on clean
   evict (`batch.StateSaverEngine`), restore + prefill-suffix when a prompt EXTENDS it.
   - C ABI `gemma4_engine_q35_state_{size,save,restore}` + `seq_ntokens` (scheduler hist
     runs one sampled-but-unfed token ahead). Snapshot = fixed ~75 MiB GDN S/rings +
     ~48 KiB/token fp32 FULL-KV. Host LRU in cuda.BatchAdapter (8 GiB budget), supersede-
     dedupe, probe-before-open, hits merged into the prefix-cache /metrics counters.
   - Gate `make qwen35-state-test`: warm continuation after CROSS-SLOT restore is
     BIT-IDENTICAL to the uninterrupted sequence (16/16). (Cold-vs-warm forks after ~2
     tokens: prefill-derived GDN state ≠ decode-committed state bitwise — fp32 non-assoc,
     both oracle-valid; documented, cold leg informational.)
   - chat/qwen deviation (documented): historical assistant turns keep the empty
     `<think>\n\n</think>\n\n` the generation committed, so re-renders stay token-exact
     and the cache keeps matching across turns. Goldens regenerated with the deviation.

3. **Tokenizer newline pre-tokenization bug — FIXED** (`c507864`), uncovered while
   debugging state-cache misses. `\s*[\r\n]+` outranks `\s+(?!\S)`: "\n\nWord" must be
   [ĊĊ=271, Word], NOT [198,198,Word]. Was diverging from HF/llama.cpp on EVERY multi-line
   Qwen prompt (every ChatML turn boundary) and breaking the token-exact re-render the
   state/prefix cache depends on. `gpt2_bpe.go`; pinned test on the real tokenizer.json.

4. **Scheduler phase telemetry — DONE** (`addfcd8`), the first tool for THE PLAN item 3.
   One log line per ≥64-step/≥5s window: avgB, engine ms/step, deliver/admit/pass ms.

## Scoreboard (measured Jul 4, same bench_serving.py protocol as the vLLM records)

| metric                | fucina served | vLLM  | status |
|-----------------------|---------------|-------|--------|
| decode single-stream  | 56–58         | 53.4  | ✅ WON |
| TTFT short            | ~82 ms        | 67 ms | ~      |
| TTFT @2k, turn 1      | 1.47 s        | 1.19s | ⚠️ close |
| **TTFT @2k, turn 2+** | **0.107 s**   | (APC weak) | ✅ NEW WIN (state cache) |
| aggregate @conc-16    | 198–242 (±8% noise) | 449* | ⚠️ |

*449 = bench identical-prompt convergent-routing caveat (see git history); not steady state.

## Serving-vs-engine gap — ANSWERED by the telemetry (THE PLAN item 3)

conc-16, 512-tok runs: **the Go scheduler adds ~0** (deliver 0.01 ms, admit 0.00 ms
steady-state, pass == engine). The gap is INSIDE the engine step: **59 → 66 ms/step as
sequences lengthen** — fp32 FULL-layer attention reads scale with position, while the
engine microbench decoded at short positions. So item 3 is NOT a Go/scheduler problem;
the lever is the engine's FULL-layer attention cost (fp32 K/V, position-linear).

## Decode-kernel push (ultracode, Jul 4) — RESULTS + what's left

Node-traced B=16 step = 58ms: grouped NVFP4 experts ~20ms (already TC, tuning only),
mixer Q4_K dp4a 13.2ms, GDN 9.2ms, LM head 6.3ms, act-quant 5.9ms, shared-expert 4.3ms.
Full findings in memory `qwen35-decode-opt-findings.md`.

- **IMMA/tensor-core mixer = DEAD END (reverted).** bf16 WMMA mixer is CORRECT (B=16
  argmax bit-identical to dp4a) but SLOWER (259 vs 274 tok/s): Q4_K's per-32-elem
  asymmetric scales force a per-weight dequant that IS the ALU cost; int8 IMMA's
  per-sub-block fold caps it the same way. dp4a is well-matched to Q4_K GEMV at low B.
- **fp16 FULL-layer KV = SHIPPED (`62986b2`), MEMORY-only.** Halves the KV arena
  (16.6→8.3 GB), gated (oracle 8/8, state 16/16). Single-stream long-ctx decode +2% only.
  **KEY FINDING: the long-context decode tax is ATTENTION-KERNEL OCCUPANCY-bound, NOT
  KV-BW-bound** — `qwen35_b_attn_kernel` is one block per (head,row) = 16 blocks at B=1.
- **spec-on-MoE = DEFERRED.** Review found the cheap mechanism broken (single-token GDN
  kernels don't thread the recurrence across K verify tokens → not lossless); needs a
  per-slot GDN/conv SANDBOX via the chunk-scan kernels + a kv_write p<maxctx clamp.

## FINALIZED PLAN

The definitive ranked plan now lives in **docs/qwen35-beat-vllm-plan.md** (committed).
Headline: the two things a decode-kernel push can win here are WON (single-stream decode +
turn-2 TTFT); flash-decoding fixed long-ctx decode (33→49 tok/s). Remaining perf is sub-15%
against physics floors — the highest-value next action is **json_schema/response_format**
(cross-branch merge of grammar core `978d91d`+`6ef429b` from feat/e4b-gguf; NOT present on this
branch), then LM-head-quant + GDN-bf16-arena (cheap decode wins), then grouped-expert tuning
(the only lever that scales with concurrency). Defer prefill / shared-expert / spec-on-MoE.

Session shipped: dialect+tool-calling, state cache, tokenizer fix, telemetry, fp16 KV,
flash-decoding, oracle opt-in. Gates green: MoE oracle 8/8, 9B batch-test, llama.cpp long-ctx
parity 40/40 @1k+4k. Memory: qwen35-decode-opt-findings.md, qwen-chat-dialect-done.md,
qwen35-state-cache-done.md.
