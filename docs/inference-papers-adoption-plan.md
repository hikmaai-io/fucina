{
  "summary": "Survey latest LLM inference papers/techniques and map them onto fucina on GB10",
  "agentCount": 6,
  "logs": [],
  "result": "# Top 8 Ranked (expected win × feasibility in fucina)\n\n## 1. Suffix decoding over generation history (Arctic Inference)\n- **What**: Persistent per-server suffix tree over prompts + prior outputs; longest-suffix match drafts adaptively long candidates; falls back to the existing n-gram drafter when tree score is low.\n- **Where**: Pure Go — extend `internal/server/batch/drafter.go` (n-gram drafter already exists) feeding the existing SpecScheduler + ragged spec-in-batch verify ABI. Zero CUDA changes.\n- **Gain**: Strict superset of the shipped 2x prompt-lookup path; 1.8–4.5x reported on agentic/multi-turn loops — exactly fucina's parallel-workflow invariant traffic. Realistic: +30–100% over current spec on repeat-heavy workloads.\n- **Risk**: Low. Tree memory growth (cap + evict); short-match garbage drafts (enforce min match length ≥3, same footgun as vLLM ngram). Losslessness preserved by the existing verify step.\n\n## 2. Fused grouped GEMM over active experts (masked layout for decode)\n- **What**: One launch per MoE layer: extend the verified batched FP8-block GEMM to take an expert-id/offset array; masked/batched layout for decode (static shapes → CUDA-graph capturable), contiguous layout for prefill; fuse gate+up+SiLU+down.\n- **Where**: `cuda/fp8_block.cuh` (primitive exists) + Qwen3.5-MoE engine path (`test_qwen35_moe_fp8_engine.cu` as harness); scheduler packing to B≥8 already aligns.\n- **Gain**: Eliminates ~1150 launches/token — the exact overhead-bound decode regime measured vs vLLM (jun27 memory). Est. +20–50% MoE decode at low/mid batch, and it's the substrate findings #9–11 need.\n- **Risk**: Medium. Masked layout wastes compute at B=1; expert-id indirection must stay graph-static (fixed slot counts). Parity oracle exists (`qwen35_moe_fp8_ref.py`).\n\n## 3. Marlin-style batched quantized decode kernel (weights read once per step, N=1→32)\n- **What**: Small-N mixed-input kernel: stream quantized weights once per step (evict-first L2 policy), keep all batch activations resident, interleave dequant→mma; striped split-K across SMs. This IS the pending \"concurrency IMMA kernel\" + \"MAX_SEQS→32\" + \"Q4_K packed GEMV\" levers.\n- **Where**: New kernel alongside `cuda/mmvq.cuh`; keep dp4a GEMV for B=1 (RaZeR: CUDA-core path wins pure GEMV); dispatch by batch M. Bench via `test_qwen35_batch_bench.cu` / `test_q4k_mmq_bench.cu`.\n- **Gain**: Aggregate tok/s ~linear in batch until compute-bound instead of re-reading 12B weight bytes per sequence — the single biggest throughput lever on 273 GB/s and required by the parallelism invariant. 2–4x aggregate at N=8–32.\n- **Risk**: Highest engineering cost in the list; sm_121 mma shape/L2-policy differences from Ampere/Hopper Marlin; needs Machete-style layout derivation rather than hand-copying.\n\n## 4. FlashQLA-style fused GDN chunked prefill + TFLA large chunks\n- **What**: (a) Fuse gating/decay elementwise stages into the GDN chunk GEMMs with warp specialization (no separate launches); (b) grow chunk 64→256–512 and tile the intra-chunk quadratic part across CTAs (TFLA), cutting [128×128] state HBM round-trips 4–8x; (c) decay-driven sequence splitting to fill SMs on one long prompt.\n- **Where**: Qwen3.5 GDN prefill kernels (profile entry `cuda/prof_qwen35_prefill.cu`); ABI/oracle from FlashInfer `chunk_gated_delta_rule` (SM100, head 128 — exact match).\n- **Gain**: Directly attacks the open \"GDN occupancy\" item and the \"beat vLLM prefill\" goal — vLLM still ships FLA Triton kernels here, so 2–3x GDN forward is a path to winning TTFT outright (4.6s → ~2s class).\n- **Risk**: Medium-high. Warp-specialization/TMA complexity on sm_121; algebraic reformulation must preserve the hard-won 8/8 token parity (oracles exist: `qwen35_layer_ref.py`, longctx tests).\n\n## 5. Dynamic draft-length stopping (SpecDec++ threshold rule)\n- **What**: Replace fixed draft k with: accumulate 1−∏(conf_i) from the drafter's own per-token confidence (n-gram match length, suffix-tree frequency, MTP top-1 prob) and stop drafting when cumulative rejection probability crosses a threshold. Also absorbs the DSpark deep-draft-then-truncate idea.\n- **Where**: `internal/server/batch/confsched.go` (confidence scheduler already exists — this is an extension, possibly small) + `drafter.go` confidence outputs.\n- **Gain**: +7–11% over best fixed k, and makes one pipeline behave across code/chat/structured traffic with zero flags (\"ship defaults not flags\"). Multiplies findings #1 and the MTP path.\n- **Risk**: Very low. Only affects draft length, never correctness (verify is lossless); threshold tuning is a one-time sweep.\n\n## 6. QTIP/EXL3 trellis 3-bpw GEMV (paired with YAQA offline rounding)\n- **What**: New mmvq-style kernel: one trellis chain per thread (compute-free codebook, ~2 ALU/weight), Hadamard un-transform fused in epilogue; quantize offline with the open EXL3/QTIP toolchain, rounding via YAQA for quality.\n- **Where**: New kernel next to `cuda/mmvq.cuh` + new tensor type in the GGUF/safetensors loaders; offline pipeline runs on the GB10's 128 GB.\n- **Gain**: The only remaining dense-12B lever per memory (\"only fewer weight bytes help\"): ~1.35x decode at 3 bpw with quality far above Q3_K. Stacked: 19.5 → ~26 plain × 2x spec ≈ **hits the 50 tok/s goal**.\n- **Risk**: High-medium. Sequential trellis walk must still reach ~80% peak BW on LPDDR5x (EXL3 proves it on HBM, not GB10); new on-disk format; quality validation burden — hence pair with YAQA and gate on PPL/parity.\n\n## 7. Split-KV decode attention with cached CPU-side plan (FlashInfer)\n- **What**: Per-step CPU `plan()` emits a deterministic load-balanced CTA→(head, KV-chunk) mapping from the batch's ragged lengths (scheduler already knows them); persistent attention kernel + fused variable-length softmax merge.\n- **Where**: `cuda/paged_kv_device.cuh` + the Gemma-4/Qwen attention decode kernels; plan lives beside the existing CUDA-graph decode step.\n- **Gain**: Bigger than the finding's generic \"single-digit %\" claim for fucina specifically: sliding-attn overhead is a measured 10.4 ms of a ~51 ms dense-12B step (~20%). Recovering half is +10% decode; also covers ragged spec-verify (multi-query tokens/seq) for free.\n- **Risk**: Low-medium. Merge-kernel overhead at very short ctx; plan must be cached/graph-compatible (recompute only when lengths change shape class).\n\n## 8. LM-head quantization (Q6_K/Q4_K through existing dp4a GEMV)\n- **What**: Stage 1 only: quantize `output.weight` to Q6_K (measure Q4_K) at load time in the E4B/Qwen/safetensors loaders and route through the existing dp4a GEMV. Skip FlashHead clustering for now.\n- **Where**: Loaders (`cuda/qwen35_fp8_loader.h`, E4B/Gemma loaders); no new kernels.\n- **Gain**: 2.7–4x fewer head bytes vs BF16 heads. Biggest on E4B (262k×2048 ≈ 537M params — dominant fixed per-token cost) and the FP8 checkpoints whose heads aren't yet 4/6-bit; ~5–10% dense decode.\n- **Risk**: Low. Quality check per model (Q6_K is near-lossless in practice); verify spec-decode logit parity on greedy tests before defaulting Q4_K.\n\n---\n\n# Explicitly REJECTED\n\n- **MoE-SpeQ / CoX-MoE expert prefetch-offload** — no PCIe boundary to hide: 35 GB FP8 MoE is fully resident in GB10's 128 GB unified memory; revisit only for >120 GB models.\n- **Prompt-lookup n-gram drafting** — already shipped in fucina (the 2x lossless path); only carry over the `min_ngram ≥ 3` structured-output footgun guard, subsumed by #1.\n- **EAGLE-3** — requires training a head per target model; violates fucina's training-free, model-agnostic constraints. Keep only as the acceptance-rate benchmark.\n- **SWIFT/CLaSp layer-skip self-speculation** — 1.3–1.6x is below the existing n-gram+MTP+dynamic-length stack; note as fallback for no-MTP checkpoints, don't build.\n- **Full megakernel / on-GPU interpreter (MPK, Hazy)** — the 2.5x numbers are 1B-on-H100 where bubbles dominate; the memory-floor study says launch overhead is already masked at 273 GB/s, and memory confirms schedule levers are dead at B=1. Only the narrow norm-into-GEMV fusion survives, and only if a floor audit proves pipeline drains.\n- **FlashKDA** — read the kernel source as design reference for #4's cross-chunk state layout; not a dependency (fucina is standalone CUDA), nothing to implement directly.\n- **MoE-Gen module-level batching** — offloading machinery N/A (resident weights); the expert-byte amortization math is real but is delivered by #2 + scheduler packing to B≥8, not by a separate mechanism.\n\n# Deferred (not rejected — sequenced behind the top 8)\n\n- **Analytic memory-floor audit** — do as a 1–2 day diagnostic before #6/#7 to attribute the 17% gap line-by-line; it's a method, not a feature.\n- **Native FP4 mma retest** (`mxf4nvf4.block_scale.scale_vec::4X.m16n8k64` from the croll83 GB10 fork) — cheap 1-day experiment that could overturn the \"sm_121 FP4 broken\" memory note; run before committing to #6's format choice.\n- **Q4_0/Q4_K offline repack (RaZeR-style)** — real but small (5–15% GEMV) and partially superseded by #3's repacked layout; fold into #3.\n- **FP8 block-scaled MMA hardware fusion + NVFP4 tensor-core prefill** — targeted upgrade to `fp8_block.cuh` for compute-bound paths; do after #2/#4 land.\n- **FP8 attention math (two-level accumulation) + NVFP4-KV tier** — long-context (>4–7k) and client-RAM win; mandatory two-level accumulator before any 64k+ correctness claim.\n- **GDN state snapshots for prefix caching + per-draft state sandboxes (vLLM/SGLang pattern)** — the sandbox half is a **correctness prerequisite** the moment MTP spec-decoding turns on for Qwen3.5 (rejected drafts corrupt in-place GDN state); schedule with the spec-serving wiring.\n- **Opportunistic expert rerouting, MoE-Spec expert budgeting, Cascade utility gating, EqSpec/EXSpec grouping** — all cheap add-ons that require #2 (grouped kernel) and/or spec-on-MoE to exist first; adopt in that order.",
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Research"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Synthesize"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "research:decode-gemv",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "af96fa98cb58cbd19",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1782982440002,
      "queuedAt": 1782982439990,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Use WebSearch/WebFetch to research: Recent (2025-2026) papers/techniques for memory-bandwidth-optimal single-token decode GEMV on quantized weights (FP8/INT4) on NVIDIA Blackwell: kernel designs, weight layouts, tensor-core batched decode (IMMA/mma.sync), split-K attention for short contexts. Find concrete algorithmic ideas fucina (a CUDA inference engine on GB10, 273 GB/s unified memory) could ad…",
      "lastProgressAt": 1782982685257,
      "tokens": 29596,
      "toolCalls": 14,
      "durationMs": 245255,
      "resultPreview": "{\"findings\":[{\"technique\":\"Megakernel / persistent-kernel decode with counter-based sync and pipelined next-op weight loads\",\"source\":\"Hazy Research 'Look Ma, No Bubbles!' (https://hazyresearch.stanford.edu/blog/2025-05-27-no-bubbles), Mirage Persistent Kernel (https://github.com/mirage-project/mirage, https://zhihaojia.medium.com/compiling-llms-into-a-megakernel-a-path-to-low-latency-inference-cf…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "research:moe-serving",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "ac79f60634fa59392",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1782982440003,
      "queuedAt": 1782982439990,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Use WebSearch/WebFetch to research: Latest (2025-2026) MoE inference serving techniques: expert grouping/batching for decode, expert-weight caching/prefetch, fused grouped GEMM for top-k experts, expert parallel token routing, speculative decoding on MoE (does it pay?). Target: Qwen3.5-35B-A3B-style 256-expert top-8 FP8 serving on a single GB10.\nReturn the 4-6 strongest, most concrete findings. Pr…",
      "lastProgressAt": 1782982661042,
      "tokens": 26767,
      "toolCalls": 12,
      "durationMs": 221039,
      "resultPreview": "{\"findings\":[{\"technique\":\"Fused grouped GEMM over active experts (token-sort + single launch per MoE layer)\",\"source\":\"vLLM fused-MoE kernel design docs (docs.vllm.ai/en/latest/design/moe_kernel_features/); FusedXpert SC'25 poster (sc25.supercomputing.org post173); DeepSeek DeepGEMM grouped-GEMM (contiguous + masked layouts)\",\"idea\":\"Never launch one GEMM per expert. Sort/align token indices by e…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "research:spec-decode",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "a9eec137dfa8cb9fd",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1782982440003,
      "queuedAt": 1782982439990,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Use WebSearch/WebFetch to research: State of the art (2025-2026) in training-free speculative decoding: prompt-lookup, self-speculation, EAGLE-3, Medusa-style heads, DeepSeek MTP, dynamic draft-length scheduling, confidence-based verify budgets. What accept-rates are reported on general text and how are drafts scheduled under continuous batching?\nReturn the 4-6 strongest, most concrete findings. P…",
      "lastProgressAt": 1782982613812,
      "tokens": 27175,
      "toolCalls": 12,
      "durationMs": 173809,
      "resultPreview": "{\"findings\":[{\"technique\":\"Prompt-lookup / n-gram drafting (vLLM 'ngram' method)\",\"source\":\"vLLM docs (docs.vllm.ai/en/latest/features/speculative_decoding/n_gram/), vLLM blog 2024-10-17-spec-decode, vLLM issue #40875\",\"idea\":\"Draft tokens by matching the last n-gram of the generated sequence against the prompt + prior output and copying the continuation; zero model, zero training, ~free to comput…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "research:prefill-hybrid",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "ab1482e29897f83f4",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1782982440003,
      "queuedAt": 1782982439990,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Use WebSearch/WebFetch to research: Recent techniques (2025-2026) for fast prefill of hybrid linear-attention models (Gated DeltaNet, Mamba2, Qwen3-Next/Qwen3.5 hybrid): chunked parallel scan, chunk-state recomputation, tensor-core kernels for delta-rule recurrence, long-context TTFT optimizations reported by vLLM/SGLang.\nReturn the 4-6 strongest, most concrete findings. Prefer primary sources (ar…",
      "lastProgressAt": 1782982630964,
      "tokens": 25249,
      "toolCalls": 13,
      "durationMs": 190961,
      "resultPreview": "{\"findings\":[{\"technique\":\"FlashQLA: fused warp-specialized GDN chunked-prefill kernels (TileLang, SM90/SM100)\",\"source\":\"https://github.com/QwenLM/FlashQLA and https://qwen.ai/blog?id=flashqla (QwenLM, 2026)\",\"idea\":\"Qwen's own kernel library reformulates the GDN chunked-prefill forward algebraically to be 'hardware-friendly' (reducing Tensor Core, CUDA Core and SFU work without precision loss), …"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "research:quantization",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "addc528db2afdbf00",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1782982440003,
      "queuedAt": 1782982439990,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Use WebSearch/WebFetch to research: Newest (2025-2026) weight quantization for inference quality+speed: NVFP4/MXFP4 on Blackwell, 2-3 bit methods with usable quality (AQLM/QuIP#/QTIP successors), FP8 block scaling, quantized KV cache, and LM-head quantization tradeoffs at ~250k vocab.\nReturn the 4-6 strongest, most concrete findings. Prefer primary sources (arXiv, vLLM/SGLang/TensorRT-LLM blogs or…",
      "lastProgressAt": 1782982680246,
      "tokens": 31239,
      "toolCalls": 15,
      "durationMs": 240243,
      "resultPreview": "{\"findings\":[{\"technique\":\"QTIP trellis-coded 2-4 bit weights (EXL3 format) — GEMV at >80% of peak bandwidth with a compute-free codebook\",\"source\":\"arXiv 2406.11235 (QTIP, NeurIPS'24 Spotlight); https://www.together.ai/blog/even-better-even-faster-quantized-llms-with-qtip; https://github.com/turboderp-org/exllamav3 (EXL3)\",\"idea\":\"Replace vector-codebook quantization (QuIP#/AQLM) with 256-dim tre…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "synthesize",
      "phaseIndex": 2,
      "phaseTitle": "Synthesize",
      "agentId": "a527ebebab3ead4d7",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1782982685258,
      "queuedAt": 1782982685258,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "grep -l \"gdn\\|GDN\\|delta\" /home/mauromedda/hack/fucina/.cla…",
      "promptPreview": "You are ranking research findings for fucina, a from-scratch CUDA inference engine on NVIDIA GB10 (DGX Spark, 273 GB/s unified memory, sm_121a) serving: Gemma-4 12B Q4_0 dense (19.5 tok/s plain B=1, GEMVs at 83% peak BW, sliding-attn 10.4ms overhead at short ctx), Qwen3.5-9B FP8 dense, Qwen3.5-35B-A3B FP8 MoE (112 tok/s @B=8), plus a lossless prompt-lookup speculative path (2x on dense). Goals: 50…",
      "lastProgressAt": 1782982854388,
      "tokens": 32482,
      "toolCalls": 2,
      "durationMs": 169129,
      "resultPreview": "# Top 8 Ranked (expected win × feasibility in fucina)\n\n## 1. Suffix decoding over generation history (Arctic Inference)\n- **What**: Persistent per-server suffix tree over prompts + prior outputs; longest-suffix match drafts adaptively long candidates; falls back to the existing n-gram drafter when tree score is low.\n- **Where**: Pure Go — extend `internal/server/batch/drafter.go` (n-gram drafter a…"
    }
  ],
  "totalTokens": 172508,
  "totalToolCalls": 68
}